# A06: Vulnerable & outdated components — pinned to versions with known CVEs so
# Trivy's dependency scan has real findings. The fixed app upgrades all of these.
Flask==0.12.2
Werkzeug==0.14.1
Jinja2==2.10
itsdangerous==0.24
requests==2.19.1
urllib3==1.24.1
PyYAML==3.13
PyJWT==1.5.0
